# Implementation Plan: Post-Review Hardening

## Overview

Seven targeted security and reliability improvements applied in dependency order. Early tasks (encryption module, env validation) establish foundations used by later tasks (storage integration, middleware). The stack is Node.js 22+ with ES Modules, Express 4.x, Mongoose 8.x, @shopify/shopify-api 11.x, tested with Vitest + fast-check.

## Tasks

- [ ] 1. Fix instanceof error detection in OAuthController
  - [ ] 1.1 Replace string-based error detection with instanceof checks
    - Import `CookieNotFound` and `InvalidOAuthError` from `@shopify/shopify-api`
    - Replace `error.constructor.name === 'CookieNotFound'` with `error instanceof CookieNotFound`
    - Replace `error.constructor.name === 'InvalidOAuthError'` with `error instanceof InvalidOAuthError`
    - Remove fallback `errorMessage.includes(...)` string checks
    - Preserve existing recovery flows (iframe breakout for CookieNotFound, 403 for InvalidOAuthError)
    - _Requirements: 1.1, 1.2, 1.3, 1.4_

  - [x]* 1.2 Write unit tests for instanceof error branching
    - Test that a thrown `CookieNotFound` instance triggers the iframe breakout redirect
    - Test that a thrown `InvalidOAuthError` instance returns HTTP 403
    - Test that an unknown error returns HTTP 500 generic message
    - _Requirements: 1.2, 1.3, 1.4_

- [ ] 2. Implement AES-256-GCM encryption module
  - [ ] 2.1 Create `src/storage/encryption.js` module
    - Implement `encrypt(plaintext, hexKey)` using AES-256-GCM with 12-byte random IV
    - Implement `decrypt(encoded, hexKey)` that parses IV + authTag + ciphertext from base64
    - Use Node.js built-in `crypto` module
    - Output format: base64(IV 12B || authTag 16B || ciphertext)
    - _Requirements: 2.3, 2.4, 2.5, 2.6_

  - [x]* 2.2 Write property test for encryption round-trip
    - **Property 1: Encryption Round-Trip Preserves AccessToken**
    - For any arbitrary non-empty string and valid 32-byte hex key, `decrypt(encrypt(s, k), k) === s`
    - Use `fc.string({ minLength: 1 })` for plaintext, fixed valid hex key
    - **Validates: Requirements 2.3, 2.4, 2.6**

  - [x]* 2.3 Write property test for IV uniqueness
    - **Property 2: Unique IV Per Encryption Operation**
    - For any plaintext and key, encrypting the same value N times produces N distinct IVs
    - Extract first 12 bytes (IV) from each base64-decoded output and assert uniqueness
    - **Validates: Requirements 2.5**

- [ ] 3. Add ENCRYPTION_KEY to environment validation
  - [ ] 3.1 Update `src/config/env.js` to require ENCRYPTION_KEY
    - Add `'ENCRYPTION_KEY'` to the `REQUIRED_VARS` array
    - Application will throw and exit if ENCRYPTION_KEY is missing at startup
    - _Requirements: 2.1, 2.2_

  - [x]* 3.2 Write unit tests for ENCRYPTION_KEY validation
    - Test that missing ENCRYPTION_KEY throws an error
    - Test that a present ENCRYPTION_KEY allows boot to continue
    - _Requirements: 2.1, 2.2_

- [ ] 4. Integrate encryption into CustomSessionStorage
  - [x] 4.1 Update `src/storage/CustomSessionStorage.js` with encrypt/decrypt
    - Import `encrypt` and `decrypt` from `./encryption.js`
    - In `storeSession`: encrypt `accessToken` if non-null before writing to MongoDB
    - In `loadSession`: decrypt `accessToken` if non-null after reading from MongoDB
    - Skip encryption/decryption when `accessToken` is null or undefined
    - Read `ENCRYPTION_KEY` from `process.env.ENCRYPTION_KEY`
    - _Requirements: 2.3, 2.4, 2.5, 2.6, 2.7_

  - [x]* 4.2 Write unit tests for CustomSessionStorage encryption integration
    - Test that storeSession writes an encrypted (non-plaintext) accessToken to the DB
    - Test that loadSession returns the original plaintext accessToken
    - Test that null/undefined accessToken passes through unchanged
    - _Requirements: 2.3, 2.4, 2.7_

- [ ] 5. Checkpoint - Verify encryption pipeline
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 6. Add raw body capture in app.js
  - [ ] 6.1 Configure express.json() verify callback in `src/app.js`
    - Add `verify: (req, res, buf, encoding) => { if (buf && buf.length > 0) req.rawBody = buf; }` option to `express.json()`
    - Remove standalone `app.use(express.urlencoded(...))` if redundant or keep if needed for form data
    - _Requirements: 3.1, 3.2, 3.3_

  - [ ]* 6.2 Write property test for raw body preservation
    - **Property 3: Raw Body Preservation**
    - For any valid JSON object generated by fast-check, sending it as a request body results in `req.rawBody` containing the exact original bytes
    - Use `supertest` to send requests to the Express app and verify `req.rawBody` matches
    - **Validates: Requirements 3.2**

  - [ ]* 6.3 Write unit test for empty body handling
    - Test that a request with no body leaves `req.rawBody` undefined
    - _Requirements: 3.3_

- [ ] 7. Add TTL index on Session expires field
  - [ ] 7.1 Update `src/models/Session.js` with TTL index
    - Add `SessionSchema.index({ expires: 1 }, { expireAfterSeconds: 0 })` after schema definition
    - This enables MongoDB automatic deletion of expired session documents
    - _Requirements: 4.1, 4.2_

  - [x]* 7.2 Write unit test verifying TTL index definition
    - Assert that the Session schema indexes include `expires` with `expireAfterSeconds: 0`
    - _Requirements: 4.1_

- [ ] 8. Add rate limiting with express-rate-limit
  - [ ] 8.1 Install `express-rate-limit` and configure limiter in `src/app.js`
    - Add `express-rate-limit` to dependencies in `package.json`
    - Import `rateLimit` from `express-rate-limit`
    - Create limiter with `windowMs: 60_000`, `max: 30`, `standardHeaders: true`, `legacyHeaders: false`
    - Apply limiter to `/` and `/api/auth` routes before mounting `authRouter`
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_

  - [ ]* 8.2 Write integration test for rate limiting
    - Test that 31st request within 60s window returns HTTP 429
    - Test that requests within limit return normal responses
    - _Requirements: 5.3, 5.4_

- [ ] 9. Implement validateSessionToken middleware
  - [ ] 9.1 Create `src/middleware/validateSessionToken.js`
    - Export `validateSessionToken` async middleware function
    - Extract Bearer token from `Authorization` header
    - Call `shopify.session.decodeSessionToken(token)` for JWT verification
    - On success: attach decoded payload to `req.sessionToken`, call `next()`
    - On failure: respond HTTP 401 with JSON error message
    - Include comprehensive JSDoc documentation with usage example
    - Do NOT mount on any route by default in the application
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6_

  - [ ]* 9.2 Write property test for valid session token verification
    - **Property 4: Valid Session Token Verification Succeeds**
    - For any JWT signed with the correct API secret and valid claims, middleware attaches payload and calls next()
    - Mock `shopify.session.decodeSessionToken` to resolve with generated payloads
    - **Validates: Requirements 6.2, 6.3**

  - [ ]* 9.3 Write property test for invalid session token rejection
    - **Property 5: Invalid Session Token Verification Rejects**
    - For any malformed/expired/incorrectly-signed JWT, middleware responds 401 and does NOT call next()
    - Mock `shopify.session.decodeSessionToken` to reject with varied errors
    - **Validates: Requirements 6.4**

- [ ] 10. Checkpoint - Verify all middleware and security features
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 11. Add GDPR webhook documentation to README
  - [ ] 11.1 Add GDPR Mandatory Webhooks section to `README.md`
    - Add a new section documenting the three mandatory GDPR webhook endpoints
    - Document `customers/data_request`: purpose and expected behavior
    - Document `customers/redact`: purpose and expected behavior
    - Document `shop/redact`: purpose and expected behavior
    - Note that these endpoints are required for Shopify App Store submission
    - _Requirements: 7.1, 7.2, 7.3_

- [ ] 12. Final checkpoint - Full test suite verification
  - Ensure all tests pass, ask the user if questions arise.

## Notes

- Tasks marked with `*` are optional and can be skipped for faster MVP
- Each task references specific requirements for traceability
- Checkpoints ensure incremental validation after major milestones
- Property tests validate universal correctness properties from the design document
- Unit tests validate specific examples and edge cases
- The `express-rate-limit` package must be installed (task 8.1) before rate limiting tests can run
- ENCRYPTION_KEY must be a 64-character hex string (32 bytes) — add to `.env.example` as documentation

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 0, "tasks": ["1.1", "2.1", "3.1", "7.1"] },
    { "id": 1, "tasks": ["1.2", "2.2", "2.3", "3.2", "4.1", "7.2"] },
    { "id": 2, "tasks": ["4.2", "6.1", "8.1", "9.1", "11.1"] },
    { "id": 3, "tasks": ["6.2", "6.3", "8.2", "9.2", "9.3"] }
  ]
}
```
